# PlayPK Dashboard

Next.js (App Router) company dashboard + admin panel — **not scaffolded yet**.

Route groups planned:
- `(company)` — venue owner / branch manager UI
- `(admin)` — platform admin UI

Will be initialized after the core REST API (auth, companies, bookings) is in place.
