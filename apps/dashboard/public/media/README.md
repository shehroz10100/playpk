# Media posters

Hero, venue, and sport surfaces use optimized still images (see `src/lib/venue-cover.ts`
and `@playpk/shared-types` sport covers). Local MP4 loops were removed to cut ~5.5MB of
unused assets and improve mobile load time.
