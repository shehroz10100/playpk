# Local media loops (Phase 3)

Short muted MP4 loops for hero / sport / venue previews (Mixkit stock, ≤2MB, ~360p).

Replace with venue-owned clips when available — keep filenames stable so `src/lib/media-assets.ts` stays valid.

| File | Use |
|------|-----|
| `hero-court.mp4` | Discover / login hero |
| `venue-preview.mp4` | Venue card hover + venue tour |
| `sport-*.mp4` | Sport filter tiles |
