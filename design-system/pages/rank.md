# Rank / leaderboard

Applies to `/rank`.

## Tone
- Scoreboard energy: ambient banner + tabular stats
- Dense table stays flat (no shader behind rows)
- `RankUpGlow` + `CountUp` for the signed-in player only

## Layout
1. AmbientPromo header
2. Your record card (points / W–L / matches)
3. Top players table

No autoplay video. Presentation only.
