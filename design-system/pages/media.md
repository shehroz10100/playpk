# Media / photography (Phase 3)

Overrides MASTER only for motion-media placement:

- **One** autoplaying loop above the fold per view (discover hero OR login desktop hero — never both stacks on mobile).
- Venue cards: static poster default; video only on hover/focus while in view.
- Sport tiles: low-opacity loop only on the **active** chip; others play on hover/focus.
- Rank / leaderboard: no video (Phase 4 motion only).
- Always respect `prefers-reduced-motion` → poster / still only.
