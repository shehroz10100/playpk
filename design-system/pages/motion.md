# Motion (Phase 4)

- UI feedback: 150–300ms (`lib/motion.ts` + Framer Motion)
- Ambient shader stays slow (Phase 2)
- Checkout / confirm: `BookingStepper` calm mode only — no celebratory loops
- Always gate with `usePrefersReducedMotion` / `useMotionSafe`
- Prefer `MotionReveal`, `MotionPress`, `CountUp`, `RankUpGlow`, `StadiumSkeleton`
